package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Pretibimb Academy branding colors - "Clean Minimalism" theme
val PretibimbAdminPrimary = Color(0xFF1A56DB) // Brand Blue from HTML
val PretibimbPrimary = Color(0xFF1A56DB)      // Brand Blue instead of Orange
val DeepNavyBackground = Color(0xFFF7F9FC)    // Soft clean light slate background
val HighContrastCard = Color(0xFFFFFFFF)      // Pure white card surfaces
val LightAppBackground = Color(0xFFF7F9FC)    // Soft clean light slate
val LightCardSurface = Color(0xFFFFFFFF)       // Pure white surfaces

val SlateTextMain = Color(0xFF0F172A)         // Dark slate 900 for premium readability
val SlateTextSecondary = Color(0xFF64748B)    // Charcoal gray
val SuccessGreen = Color(0xFF10B981)          // Correct answers/Success states
val DangerRed = Color(0xFFEF4444)             // Alert/Incorrect states
val LiveRed = Color(0xFFEF4444)               // Pulsing live indicator (Red-500)
val InfoBlue = Color(0xFF1A56DB)              // Useful link badges

